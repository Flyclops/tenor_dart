/// Reduce the Number of GIF formats returned in the GIF_OBJECT list.
enum TenorMediaFilter {
  /// Formats returned: tinygif, gif, mp4
  minimal,

  /// Formats returned: nanomp4, tinygif, tinymp4, gif, mp4, nanogif
  basic,
}
